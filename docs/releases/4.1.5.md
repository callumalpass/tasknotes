# TaskNotes 4.1.5

Small fix for property-based task identification.

## Fixed

- When tasks are identified via a frontmatter property, creating a task without tags no longer injects an automatic `task` tag. Tags stay untouched in property mode.
