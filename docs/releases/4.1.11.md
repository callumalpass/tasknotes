# TaskNotes 4.1.11

Kanban subtasks now show the same custom fields as their parent cards.

## Fixed

- Subtasks expanded under a project inherit the Kanban-visible properties (e.g., custom `weight`), matching their top-level rendering.
